interface:
  display_name: "LLM Kernel Paper Writing"
  short_description: "Evidence-grounded AI systems paper argumentation"
  default_prompt: "Use $llm-kernel-paper-writing to derive and audit my paper's argument, positioning, evidence, and prose."

policy:
  allow_implicit_invocation: true
